[Unit]
Description=BYOB Server - Open Source 
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=~/byob
ExecStart=./web-gui/startup.sh > ~/bootspool.log
ExecStop=/bin/kill -s QUIT $MAINPID
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
